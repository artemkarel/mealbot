[Unit]
Description=Meal plan web
After=network.target

[Service]
WorkingDirectory=/opt/mealplan
EnvironmentFile=/opt/mealplan/.env
ExecStart=/opt/mealplan/venv/bin/uvicorn app.main:app --host 127.0.0.1 --port 8080
Restart=always

[Install]
WantedBy=multi-user.target
